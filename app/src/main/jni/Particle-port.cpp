#include <jni.h>

// added to see if the program is using this file instead of gl_code.cpp
/*#include <android/log.h>

#include <GLES2/gl2.h>
#include <GLES2/gl2ext.h>

#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <string.h>

#define  LOG_TAG    "particle-port"
#define  LOGI(...)  __android_log_print(ANDROID_LOG_INFO,LOG_TAG,__VA_ARGS__)
#define  LOGE(...)  __android_log_print(ANDROID_LOG_ERROR,LOG_TAG,__VA_ARGS__)

extern "C" {
	JNIEXPORT void JNICALL initialize(JNIEnv * env, jobject obj, jstring strVertexSrc,
			jstring strFragmentSrc);
};

// good place to create textures, or pass in from java side (resource -> byte array -> void* ?)
JNIEXPORT void JNICALL initialize(JNIEnv * env, jobject obj, jstring strVertexSrc,
		jstring strFragmentSrc)
{
//	  char buf[128];
	  //const char *str = env->GetStringUTFChars(strVertexSrc, strVertexSrc, 0);
	  //printf("%s", str);
	  //(*env)->ReleaseStringUTFChars(env, prompt, str);

}*/
